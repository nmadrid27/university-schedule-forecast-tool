# Use a flat (hoisted) node_modules so Turbopack resolves modules reliably on
# Windows CI. pnpm's default symlinked .pnpm layout is not traversed by
# Turbopack's resolver on Windows, which breaks `next build` with spurious
# "Module not found" errors for packages that are in fact installed.
node-linker=hoisted
